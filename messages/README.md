# メッセージ（翻訳）JSON

## 改行可能位置の指定

エディタで改行候補を分かりやすくするため、**`‖`**（U+2016、ダブル縦線）を改行可能位置として使います。

- **JSON 内**: `‖` を入れると、その位置で改行してよいと示せます（表示はされません）
- **読み込み時**: `i18n.ts` で `‖` はゼロ幅スペース（U+200B）に置換され、ブラウザの改行候補として扱われます

例: `"「計画は‖立てたのに、結局‖やれなかった...」"` → エディタでは ‖ の位置が一目で分かり、サイト上ではその位置で改行可能になります。
